{
  "mcpServers": {
    "plaid": {
      "command": "uvx",
      "args": [
        "mcp-server-plaid",
        "--client-id",
        "${env:PLAID_CLIENT_ID}",
        "--secret",
        "${env:PLAID_SECRET}"
      ]
    }
  }
}